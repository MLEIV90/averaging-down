import streamlit as st
st.set_page_config(page_title="Quant Strategy Engine",page_icon="📈",layout="wide")
st.title("Quant Strategy Engine")
st.caption("EOD systematic research platform — initial architecture")
cols=st.columns(4)
cols[0].metric("Universe","SPY / BTC / GLD"); cols[1].metric("Mode","RESEARCH")
cols[2].metric("Live Trading","DISABLED"); cols[3].metric("Capital","€1,100")
st.divider()
st.header("System status")
st.write("Architecture initialized. Next: validated market data → features → regime → alpha → risk → portfolio.")
st.warning("No live orders are generated by this version.")
